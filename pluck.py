def ci(citree, comptree, self):
  self.citree = citree
  self.comptree = comptree

  self.citree = [50,0,52] # offset for placematic javascript object (citree) 
  self.citree.fab = {"return stmt: \\", "stats", "is fsb/FAB connection"}
  return ci
